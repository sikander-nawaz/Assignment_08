// Write a js program to input any alphabet and check whether it is vowel or consonant.

// VALUES 
let alphabet = "A";

// SOLUTION
// USING IF CONDITION
if(alphabet === "a" || alphabet === "A" || alphabet === "e" || alphabet === "E" || alphabet === "i" || alphabet === "I" || alphabet === "o" || alphabet === "O" || alphabet === "u" || alphabet === "U"){
    console.log("Vowel Alphabet");
}else{
    console.log("Constant Alphabet");
}